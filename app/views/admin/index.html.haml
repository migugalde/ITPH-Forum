-if flash[:notice]
  .callout.warning{"data-closable" => ""}
    =flash[:notice]
    %button.close-button{"aria-label" => "Dismiss alert", "data-close" => "", :type => "button"}
      %span{"aria-hidden" => "true"} ×
%table#users
  %thead
    %tr
    %th First Name 
    %th Last Name 
    %th Email 
    %th Admin?
    %th Approved?
    %th 
  %tbody
    - @users.each do |user|
      %tr
        %td= user.first_name
        %td= user.last_name
        %td= user.email 
        %td= user.admin
        %td= user.approved
        %td= link_to 'EDIT', edit_admin_path(user)
        
%br= link_to 'sign out', destroy_user_session_path, :method => :delete, :action => :destroy
