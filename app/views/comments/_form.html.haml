= tinymce_assets
= simple_form_for([@post, @post.comments.build]) do |f|
    = f.text_area :comment, required: "true", class: "tinymce"
    .right-align
        = f.submit "Comment", class: "button"
= tinymce

